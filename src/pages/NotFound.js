import React from "react";
import styles from "../styles/NotFound.module.css"; // Import the CSS Module

const NotFound = () => {
  return <h1 className={styles.error}>404 Not Found</h1>;
};

export default NotFound;
